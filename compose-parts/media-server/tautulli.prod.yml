# Tautulli - Previously PlexPy. Plex statistics and monitoring
# Set HTTP Root in Tautulli settings if using PathPrefix
services:
  #-> https://hub.docker.com/r/linuxserver/tautulli
  tautulli:
    image: linuxserver/tautulli:latest
    container_name: tautulli
    restart: unless-stopped
    #depends_on:
    #  - plex???
    networks:
      - t2_proxy
    security_opt:
      - no-new-privileges:true
    #ports:
    #  - "${_HYPERION_TAUTULLI_LAN_PORT_HTTP}:${_HYPERION_TAUTULLI_INTERNAL_PORT}"
    volumes:
      - ${_HYPERION_STORAGE_DATA}/tautulli/config:/config
      - ${_HYPERION_STORAGE_DATA}/plexms/config/Library/Application\ Support/Plex\ Media\ Server/Logs:/logs:ro
      - ${_HYPERION_STORAGE_DATA}/shared:/shared
    environment:
      - PUID=${_HYPERION_UID}
      - PGID=${_HYPERION_GID}
      - TZ=${_HYPERION_TIMEZONE}
    labels:
      - "traefik.enable=true"
      ## HTTP Routers
      - "traefik.http.routers.tautulli-rtr.entrypoints=https"
      - "traefik.http.routers.tautulli-rtr.rule=Host(`${_HYPERION_TAUTULLI_WAN_DOMAIN_PREFIX}${_HYPERION_NETWORK_WAN_DOMAIN}`)"
      - "traefik.http.routers.tautulli-rtr.tls=true"
      ## Middlewares
      - "traefik.http.routers.tautulli-rtr.middlewares=chain-oauth@file"
      ## HTTP Services
      - "traefik.http.routers.tautulli-rtr.service=tautulli-svc"
      - "traefik.http.services.tautulli-svc.loadbalancer.server.port=${_HYPERION_TAUTULLI_INTERNAL_PORT}"
